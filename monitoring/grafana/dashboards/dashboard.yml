# Grafana dashboard provisioning for Job Matching Platform
apiVersion: 1

providers:
  - name: 'Spring Boot Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards