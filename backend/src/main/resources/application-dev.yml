spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

logging:
  level:
    com.jobmatch: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
