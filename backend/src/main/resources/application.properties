# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/jobmatch_db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

# Server Configuration
server.port=8080

# App Name
spring.application.name=job-matching-platform

# File Upload Size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.jobmatch=DEBUG
logging.level.org.springframework.security=DEBUG
```

### Step 6 ? Press Ctrl + S to save
### Step 7 ? Click green ?? button to run
### Step 8 ? Check bottom panel shows
```
